# cli

First-class TypeScript CLI for the Cloudflare Test control plane API

A shared package of the cloudflaretest workspace, consumed by the components
below at build time.

## Depended on by

- (none)
